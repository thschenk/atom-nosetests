{
  "name": "python-nosetests",
  "main": "./lib/python-nosetests",
  "version": "0.0.0",
  "description": "Run python nosetests from within the atom editor",
  "keywords": [
  ],
  "activationCommands": {
    "atom-workspace": "python-nosetests:run"
  },
  "repository": "https://github.com/atom/python-nosetests",
  "license": "MIT",
  "engines": {
    "atom": ">=0.174.0 <2.0.0"
  },
  "dependencies": {
  }
}
